@extends('layouts.app')
@section('title', 'Community')
@section('content')<div class="mx-auto max-w-7xl"><h1 class="text-3xl font-black">Community</h1><p class="mt-2 text-sm text-slate-500">Review, publish, hide, or remove church community questions.</p>@if(session('status'))<div class="mt-5 rounded-xl border border-emerald-200 bg-emerald-50 px-4 py-3 text-sm text-emerald-700">{{ session('status') }}</div>@endif<div class="mt-7 space-y-4">@forelse($questions as $question)<div class="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div class="flex flex-wrap justify-between gap-3"><div><span class="text-xs font-bold uppercase tracking-wide text-violet-600">{{ $question->category }}</span><h2 class="mt-1 text-lg font-black">{{ $question->title }}</h2><p class="mt-2 text-sm leading-6 text-slate-600">{{ $question->body }}</p></div><div class="flex items-start gap-2"><form method="POST" action="{{ route('support.community.update', $question) }}">@csrf @method('PATCH')<select name="status" onchange="this.form.submit()" class="rounded-lg border border-slate-200 px-3 py-2 text-xs"><option @selected($question->status==='pending_review')>pending_review</option><option @selected($question->status==='published')>published</option><option @selected($question->status==='answered')>answered</option><option @selected($question->status==='hidden')>hidden</option></select></form><form method="POST" action="{{ route('support.community.delete', $question) }}">@csrf @method('DELETE')<button class="rounded-lg border border-rose-200 px-3 py-2 text-xs font-bold text-rose-600">Delete</button></form></div></div></div>@empty<div class="rounded-2xl border border-dashed border-slate-300 p-10 text-center text-sm text-slate-500">No community questions yet.</div>@endforelse{{ $questions->links() }}</div></div>@endsection
