@extends('layouts.app')
@section('title', 'Live Support')
@section('content')<div class="mx-auto max-w-7xl"><h1 class="text-3xl font-black">Live Support</h1><p class="mt-2 text-sm text-slate-500">Monitor incoming conversations and update their status.</p>@if(session('status'))<div class="mt-5 rounded-xl border border-emerald-200 bg-emerald-50 px-4 py-3 text-sm text-emerald-700">{{ session('status') }}</div>@endif<div class="mt-7 space-y-4">@forelse($messages as $message)<div class="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div class="flex flex-wrap items-start justify-between gap-4"><div><div class="text-xs font-bold text-violet-600">{{ $message->installation_id }}</div><p class="mt-2 text-sm leading-6">{{ $message->body }}</p><p class="mt-2 text-xs text-slate-400">{{ $message->created_at->diffForHumans() }}</p></div><form method="POST" action="{{ route('support.live.update', $message) }}">@csrf @method('PATCH')<select name="status" onchange="this.form.submit()" class="rounded-lg border border-slate-200 px-3 py-2 text-xs"><option @selected($message->status==='open')>open</option><option @selected($message->status==='assigned')>assigned</option><option @selected($message->status==='resolved')>resolved</option><option @selected($message->status==='closed')>closed</option></select></form></div></div>@empty<div class="rounded-2xl border border-dashed border-slate-300 p-10 text-center text-sm text-slate-500">No live support messages yet.</div>@endforelse{{ $messages->links() }}</div></div>@endsection
