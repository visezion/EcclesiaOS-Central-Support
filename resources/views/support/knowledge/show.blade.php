@extends('layouts.app')
@section('title', $article->title)
@section('content')
<div class="mx-auto max-w-4xl"><div class="flex flex-wrap items-center justify-between gap-3"><a href="{{ route('support.knowledge') }}" class="text-sm font-bold text-violet-600 hover:text-violet-800">← Back to Knowledge Base</a><div class="flex items-center gap-3"><a href="{{ route('support.knowledge.edit', $article) }}" class="rounded-xl bg-violet-600 px-4 py-2.5 text-sm font-bold text-white hover:bg-violet-700">Edit article</a><form method="POST" action="{{ route('support.knowledge.delete', $article) }}" onsubmit="return confirm('Delete this article permanently?')">@csrf @method('DELETE')<button class="rounded-xl border border-rose-200 px-4 py-2.5 text-sm font-bold text-rose-600 hover:bg-rose-50">Delete</button></form></div></div>
@if(session('status'))<div class="mt-6 rounded-xl border border-emerald-200 bg-emerald-50 px-4 py-3 text-sm font-medium text-emerald-700">{{ session('status') }}</div>@endif
<article class="mt-7 overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div class="border-b border-slate-100 bg-gradient-to-br from-violet-50 to-white px-6 py-8 sm:px-10"><div class="flex flex-wrap items-center gap-3"><span class="rounded-full bg-violet-100 px-3 py-1 text-xs font-black text-violet-700">{{ $article->category }}</span><span class="inline-flex items-center gap-1.5 text-xs font-bold {{ $article->published ? 'text-emerald-600' : 'text-slate-500' }}"><span class="size-2 rounded-full {{ $article->published ? 'bg-emerald-500' : 'bg-slate-300' }}"></span>{{ $article->published ? 'Published' : 'Draft' }}</span></div><h1 class="mt-4 text-3xl font-black tracking-tight text-slate-950 sm:text-4xl">{{ $article->title }}</h1><p class="mt-3 text-xs text-slate-500">Last updated {{ $article->updated_at?->format('M d, Y \a\t H:i') }}</p></div><div class="whitespace-pre-wrap px-6 py-8 text-sm leading-7 text-slate-700 sm:px-10 sm:text-base">{{ $article->body }}</div></article></div>
@endsection
