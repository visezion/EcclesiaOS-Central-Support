@extends('layouts.app')
@section('title', 'Central Connection')
@section('content')<div class="mx-auto max-w-7xl"><h1 class="text-3xl font-black">Central Connection</h1><p class="mt-2 text-sm text-slate-500">Manage the EcclesiaOS installations allowed to use this support service.</p><div class="mt-7 overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><table class="w-full text-left text-sm"><thead class="border-b border-slate-100 text-xs uppercase tracking-wide text-slate-400"><tr><th class="p-5">Church</th><th class="p-5">Installation ID</th><th class="p-5">Version</th><th class="p-5">Last seen</th><th class="p-5">Status</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($installations as $installation)<tr><td class="p-5 font-semibold">{{ $installation->church_name ?: 'Unnamed church' }}</td><td class="p-5 text-slate-500">{{ $installation->installation_id }}</td><td class="p-5 text-slate-500">{{ $installation->version ?: '—' }}</td><td class="p-5 text-slate-500">{{ $installation->last_seen_at?->diffForHumans() ?: 'Never' }}</td><td class="p-5"><span class="rounded-full px-2.5 py-1 text-xs font-bold {{ $installation->enabled ? 'bg-emerald-50 text-emerald-700' : 'bg-slate-100 text-slate-500' }}">{{ $installation->enabled ? 'Enabled' : 'Disabled' }}</span></td></tr>@empty<tr><td colspan="5" class="p-10 text-center text-slate-500">No installations connected.</td></tr>@endforelse</tbody></table></div>{{ $installations->links() }}</div>@endsection
